/*
document.addEventListener('DOMContentLoaded', function() {
    const leftBox = document.querySelector('.box-left');
    const rightBox = document.querySelector('.box-right');
    const leftParagraph = leftBox.querySelector('p');
    const rightParagraph = rightBox.querySelector('p');

    leftBox.addEventListener('mouseover', function() {
        rightParagraph.style.opacity = '0';
    });

    leftBox.addEventListener('mouseout', function() {
        rightParagraph.style.opacity = '1';
    });

    rightBox.addEventListener('mouseover', function() {
        leftParagraph.style.opacity = '0';
    });

    rightBox.addEventListener('mouseout', function() {
        leftParagraph.style.opacity = '1';
    });
});
*/